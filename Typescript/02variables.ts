var price: number;
var x, y: number;

var price2: number, msg:string;

var test:number;

test = 1200;
// test = "1200";
// msg = 1200;

var t2:any;

t2 = 1200;
t2 = "1200";

// JS 문자열 + 숫자 ---> 문자열 + 문자열로 변경되어 연결 연산자 처리
// TS 문자열 + 숫자 ---> 문자열 + 문자열로 변경되어 연결 연산자 처리
var strTest:string = '100';
alert(strTest+4);



